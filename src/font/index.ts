import localFont from 'next/font/local'

const tiempos = localFont({
  src: '../assets/tiempos-headline-light.woff2',
  variable: '--font-geist-sans',
  weight: '100 900',
})

export default {
  tiempos,
}
